import { MapGeneratorOptions } from "./MapGenerator";

export const FroggerMapGeneratorOptions = new MapGeneratorOptions({
        pavementChance: 0.3,
        roadChance: 0.7,
        roadCarsSpeedMin: 1,
        roadCarsSpeedMax: 3
});